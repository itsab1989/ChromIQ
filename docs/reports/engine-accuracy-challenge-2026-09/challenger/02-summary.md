# Challenger summary — independent verification, 2026-09-05 04:53–05:05 (finished inside the 40-min budget; all seven items reached)

Branch feature/engine-accuracy-challenge @ 0e86b2f6 vs master 1b9cad54. No repo file edited or staged; master checked out as a throwaway worktree (removed afterwards). Details and every number: reports/challenger/01-verdicts.md. Scripts and raw logs: ~/Desktop/ChromIQ-engine-challenge/work-X/.

| # | claim | verdict | the deciding number | repro |
|---|---|---|---|---|
| C1a | colprof path untouched | CONFIRMED | `git diff 1b9cad54 0e86b2f6 -- workflow/profile_builder.py core/` is empty | shell |
| C1b | identical colprof args, beta OFF | CONFIRMED | Guided and Manual on both trees: `-D c1b -al -qm -A ChromIQ -M c1b -S …ClayRGB1998.icm <run>/c1b` — byte-identical | work-X/c1b_args.py <root> <tag> |
| C1c | only two beta-OFF behaviour changes | PARTLY | the two admitted ones are real; four more exist (precondition text, scanner preview calling choose_builder, quit-path engine guards, hidden heading) — all harmless. The admitted archive-before-rebuild also sweeps `calibrated.icc` into `old/`, which the build does not recreate (user must re-run applycal) and leaves an `old/<timestamp>/` per rebuild; the -k reset is a genuine fix that changes colprof's `-k` after a run switch for CMYK users | diff of ui/ + c6.log preview lines |
| C2 | net improvement except S4 B2A +8.8 % | CONFIRMED | at 20k eval: S4 B2A median 0.485 → 0.526 (+8.4 %), S4 A2B p95 1.508 → 1.254 (−17 %); S1–S3 all within ±1.5 % (noise). "Net improvement" = one printer's A2B; the last three changes moved nothing measurable on S1–S3 | `python -m benchmarks.battery --printers S1,S2,S3,S4 --eval 20000` → work-X/final-q.json |
| C3 | paper white and black exact, both modes, Lab+XYZ PCS | CONFIRMED via xicclu / PARTLY otherwise | xicclu: white ≤ 4.2e-5 device error in all 8 builds and 3 intents, RGB black ≤ 1e-5. littleCMS 8-bit: 1–2/255 at white, up to 5/255 at black — colprof's own profiles show the same (1/255, 5/255) through that CMM, so it is the 8-bit Lab leg. CMYK black: Maximum accuracy's is 1.5 L* LIGHTER than Fast's (14.9 vs 13.4) | work-X/c3_whiteblack.py → c3.json |
| C4 | the hue-preserving clip cannot flip hue | CONFIRMED (no flip) | S1 -qm accurate: max 7.57° over 24 far-out hues + the two historic flip colours (2.9°, 3.8°). S2 with blue ≤ 0.3: max 19.97°, 5/26 over 10°, no crossing into another family — but the worst case collapses C 110 → 12 and lifts L 55 → 78 | work-X/c4_hue.py → c4.json |
| C5 | ETA never grows between lines | CONFIRMED literally / REFUTED as useful | 924p accurate -S ClayRGB, 56.5 s: shown "~20s" at 26 % with 47 s left; no number at 40 % ("colprof time not counted") yet colprof's 44 s ran the deadline out → "taking longer"; "~20s" at 78 % with 0.3 s left; then "100 % · taking longer than estimated" on 27 lines while three sub-passes still ran. Never within 2× of the truth | work-X/c5_build.py → c5-progress.log |
| C6 | the scanner tool follows Preferences | CONFIRMED | beta ON + Maximum accuracy → EngineProfileBuilder; built in 54 s; log line "Profile check complete, peak err = 2.062908, avg err = 0.188830" matched by `_PROFCHECK_RE` | work-X/c6_scanner.py → c6.log |
| C7 | Maximum accuracy not yet a proven gain over Fast or colprof | PARTLY | synthetic referee, -qm, 20k: vs Fast better on all 8 median cells; vs colprof better on S1/S3/S4 (A2B medians −25/−18/−14 %, B2A −52/−53/−34 %) but WORSE on S2 A2B (0.220 vs 0.136 median, p95 1.79 vs 0.61 = 2.9×). Right about the print (no referee exists); on synthetics the gain is real but not uniform | work-X/battery_mode.py fast …; work-X/colprof_score.py → colprof/colprof-q.json |

Three things Basti should weigh that the orchestrator's report does not say:
1. S2 (matte, dot gain 0.70) is a printer where colprof's A2B beats Maximum accuracy by 1.6× at the median and 2.9× at the 95th percentile. One of four synthetic printers is a loss, on the table users check with profcheck.
2. Maximum accuracy's CMYK black is 1.5 L* lighter than Fast's on S3 at -ql, both exactly on the TAC. "Dark shadows keep their depth" (the Preferences text) is not what the numbers show there.
3. The remaining-time estimate is monotonic and wrong: it reads "~20s" both when 47 s remain and when 0.3 s remain, and ends on "100 % · taking longer than estimated". Monotonic was the fix for B-08; a number that never approaches the truth is the next complaint.

Scope limits: every measurement here is synthetic or the 924p developer chart; nothing was printed. littleCMS was reached only at 8 bits (PIL has no 16-bit multichannel). The "three last changes" could not be isolated from the artefacts on disk (battery-bisect.log bisects an earlier pair).
