# R-027 F-027 a loaded patch set stays armed after another project is opened
Verdict: PARTIAL (the arming and its effect on the NEXT Generate are OBSERVED; the cross-project press is still open, see R-105)
Grade: OBSERVED (R05 D). External 304-patch .ti1 loaded through the header icon into R2-Engine, "Build it as a new run instead": run2 built from it (306 with fill-up; 51 pages because the panel held a 60 mm patch-first layout from an earlier step). `_preset_ti1_path` remained the external file. The next Generate in that state laid out the SAME external set again without running targen ("chart build (Generate Chart) in manual: patch set targen" followed by "(live preview) ... patch set external_patchset.ti1" in the log, no targen Run line), with the info line saying "Auto patch count".
The cross-project step: my project switch used the session-restore route (as Agent 1's drivers do), from R2-Engine run2 to R2-High whose current run is also run2; the bar was told `set_profile_run("run2")`, nothing changed, and Create Chart stayed on R2-Engine (name field, location line, chart) while the file manager and the Measure tab had switched to R2-High. Generate then hit the typed-name gate ("You already have a project with this name") and built in R2-Engine. So no foreign build landed in R2-High, and F-027's cross-project claim is neither confirmed nor refuted by this run. Tested again through the real Open Project route in R06 (see R-105 and checkpoint 03).
Severity: medium as filed is right for what is proven (an armed set silently replacing targen on the next Generate in the same project, with the estimate and info line describing a targen build).
