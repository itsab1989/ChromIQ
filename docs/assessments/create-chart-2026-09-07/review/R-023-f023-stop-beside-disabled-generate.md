# R-023 F-023 Stop shown beside a disabled Generate when nothing runs
Verdict: CONFIRMED
Grade: OBSERVED
Severity: low as filed; but it is the same defect as F-024 (Stop follows `_chart_build_in_flight`, which reads the button), so it should be folded into F-024 rather than fixed separately. Seen on every leaked screen in R03, including another project.
