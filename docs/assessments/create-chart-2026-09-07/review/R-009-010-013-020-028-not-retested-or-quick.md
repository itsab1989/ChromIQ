# R-009 / R-010 / R-013 / R-020 / R-028
- F-009 CONFIRMED (R08, OBSERVED): SpectroScan flat A4: "Use instrument margins" visible and ticked, the four boxes locked at 6.0/6.0/6.0/11.1, the box has no tooltip. Screenshot R08-lows/f009-SS-margins-group.png. Spec: dev_margin_inspector.md "a missing combo -> no check" describes the inspector, not the panel; silent on the checkbox. Low (agree); owner choice A/B/C stands.
- F-010 NOT RETESTED on screen (needs the Preferences ruler edit again; time went to the engine). Code read agrees with Agent 1: `_chart_own_margins` returns four margins and no "ruler" key, so `_update_margin_inspector` falls back to the engine's 240. INFERRED agree. Low.
- F-013 NOT RETESTED (needs an image file in the clip band). Code read agrees: `raster.render_clip_strip` image branch draws the logo and then overlays `clip_text_lines(text)`. INFERRED agree. Low, owner question stands.
- F-020 NOT RETESTED (a 6-minute build; the spin ranges 0..200 and 0..500 are confirmed in the panel code). Agent 1's evidence (410 s, 49,590 patches of 1.02 mm) is consistent with the estimate I saw for a 1.0 mm grid. The preflight floor Agent 1 wants exists in `preflight.check` and is already used by Preferences > Chart Layout (see R-014). Medium (agree).
- F-028 NOT RETESTED (German pass; low; one language pass was Agent 1's task and its evidence is a font-metrics count I have no reason to doubt after reproducing the English clipping in R-026).
