# R-104 NEW a run's attached patch set survives a project switch through the session-restore path, and its presentation survives the real Open Project route
Area: header (Load patch set) / presets / run bar
Grade: OBSERVED
Severity: medium (session-restore path), low (real route: stale row only)
Actual (R05): Demo-Full-RGB opened (run4: log "Create Chart: this run's own patch set (Demo-Full-RGB.ti1) is attached, so regenerating reproduces it"), then R2-Engine opened through `_restore_last_session` (a real switch, run4 -> run1). On R2-Engine run1 the "Edit patch recipe (override preset)" row appeared, Pages was greyed, the estimate column read 315 / 1 page under a 9513-patch, 22-page chart (shot R05/b01-f021-after-reopen-pages1.png), and Generate laid out Demo-Full-RGB's 304-patch set into R2-Engine run1 without running targen (log: "chart build (live preview) in manual: patch set Demo-Full-RGB.ti1"; R2-Engine run1 became 315 patches). R06 through the real Open Project route (`open_project_manifest`): the armed path is cleared, Generate ran targen for R2-Guided's own set, but the override row still showed on R2-Guided.
Why it matters: the session-restore path is not one a user takes between projects in one session (it runs at app start with no project open), so the foreign build is a driver-only reproduction today; but the state that makes it possible (an armed `_preset_ti1_path` that nothing in `_restore_last_session` clears) is real, and any future caller of that path (an import, a recovery) inherits it. The stale override row on the real route tells a user a preset locks the panel when nothing does.
Spec: per_target_settings.md §2 L2 ("a project change replaces the target"); agrees that nothing of the old target may remain.
Relation to F-027: this is the mechanism behind what Agent 1 photographed; the cross-project build it inferred does not happen through Open Project.
Possible solutions (no code): clear the armed set and the override presentation in one place that both routes call (`open_project_manifest` already does "ten state resets"; `_restore_last_session` does not).
