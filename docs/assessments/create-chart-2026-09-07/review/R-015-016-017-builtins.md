# R-015 / R-016 / R-017 built-in presets
Verdicts: F-015 CONFIRMED, F-016 CONFIRMED, F-017 CONFIRMED (and broader)
Grade: OBSERVED (R05, project R2-Engine; presets picked through the combo's activated signal, builds waited for).
- F-015: after "ColorMunki A3+-1160p-1page TC9.18 extended greys by Pharmacist" the info line read "Manual mode ... targen -d2 -f9513 -e8 -B8 -G -g128 chart / printtarg -ii1 -pA4 -t300 -m10 -M10 -R 99999 -c chart": an i1 A4 printtarg line with the previous chart's 9513 under a ColorMunki A3+ prebuilt chart. After the i1Pro 162p full-layout built-in (engine on): "Built-in preset, fixed patch set ... printtarg -ii1 -pA4 -t200 -a0.95 -m10 -M10 -R 99999 -c chart". Wrong tool named in both cases.
- F-016: prebuilt built-in: engine True -> False, silently; full-layout: False -> True; "none": toggle stays on and the panel keeps the built-in's 200 dpi and margins 38/4/19/26 (the info line then says "200 dpi").
- F-017: not only the i1Pro built-ins. The ColorMunki A3+ "by Pharmacist" chart is red against the shipped ColorMunki seeds: "Left margin 5.3 mm is below the 6 mm instrument minimum" (shot c-builtin-tc918eg.png). dev_margin_inspector.md says the CM seeds were derived FROM the shipped ColorMunki presets "rounded just below the smallest known-good margin so those presets read OK out of the box"; this one does not, by 0.7 mm. Either the seed derivation missed this preset or the preset changed after the seeds were made. Owner question (Agent 1's Q12) should include the ColorMunki built-ins.
Severities: F-015 medium (agree), F-016 low (agree), F-017 medium (agree).
Spec: docs/dev_builtin_presets.md "The Manual info box says which mode it's in" is not met for the prebuilt kind.
